import React from 'react';
import { View } from 'react-native';

// Profile screen removed — minimal placeholder to avoid build/runtime issues
export default function ProfileScreen() {
  return <View />;
}
